export type EndpointKeys = keyof typeof endpoints;

export const endpoints = {
  '/stories': '/stories',
  '/intro-stories': '/intro-stories',
  '/images-scenes': '/images-scenes',
  '/story-scenes': '/story-scenes',
}